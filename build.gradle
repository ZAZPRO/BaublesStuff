
/*
 * This class (build.gradle) was created by <zazpro>. It's distributed as
 * part of the Baubles Stuff Mod. Get the Source Code in github:
 * https://github.com/ZAZPRO/BaublesStuff
 *
 * Baubles Stuff is Open Source and distributed under the
 * Baubles Stuff License: https://github.com/ZAZPRO/BaublesStuff/blob/master/LICENSE.MD
 *
 * © 2016 zazpro
 */

//Buildscript copied from Thaumic Tinkerer build.gradle, a large part of which was made by nekosune
buildscript {
    repositories {
        mavenCentral()
        maven {
            name = "forge"
            url = "http://files.minecraftforge.net/maven"
        }
        maven {
            name = "sonatype"
            url = "https://oss.sonatype.org/content/repositories/snapshots/"
        }
        maven {
            url 'https://oss.sonatype.org/content/groups/public'
        }
        mavenLocal()
    }
    dependencies {
        classpath 'net.minecraftforge.gradle:ForgeGradle:2.1-SNAPSHOT'
    }
}


apply plugin: 'net.minecraftforge.gradle.forge'

sourceCompatibility = 1.8
targetCompatibility = 1.8

<<<<<<< HEAD
version = "2.0.10"
=======
version = "2.0.9"
>>>>>>> origin/master
group = "md.zazpro.mod" // http://maven.apache.org/guides/mini/guide-naming-conventions.html
archivesBaseName = "baublesstuff"

minecraft {
    version = "1.9-12.16.1.1887"
    runDir = "run"
    mappings = "snapshot_20160312"
    replace '@version@', project.version
    replace '@keySecret@', System.env.SECRET != null ? "$System.env.SECRET" : ''

}

if (System.getenv("BUILD_NUMBER") != null)
    version = "${System.getenv().BUILD_NUMBER}"
if (project.hasProperty("releaseType"))
    if (project.releaseType == "ALPHA")
        version = "alpha-" + version


jar {
    manifest {
        attributes 'FMLAT': 'baubles_at.cfg'
    }
}

dependencies {
    compile files("lib/Baubles-1.9-1.2.0.0-deobf.jar")
	compile files("lib/Rarmor-0.7.1.jar")
}

processResources
        {
            from(sourceSets.main.resources.srcDirs) {
                include 'mcmod.info'
                expand 'version': project.version, 'MCversion': project.minecraft.version
            }
            from(sourceSets.main.resources.srcDirs) {
                exclude 'mcmod.info'
            }
        }

task deobfJar(type: Jar) {
    from sourceSets.main.output
    classifier = 'dev'
}

sourceSets {
    main {
        java {
            srcDir 'src/main/java'
        }
        resources {
            srcDir 'src/main/resources'
        }
    }
}